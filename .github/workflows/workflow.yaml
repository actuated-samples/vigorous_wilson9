name: workflow

# Generated by alexellis/actuated-batch at: 2023-12-22 09:51:24.356849 +0000 GMT m=+0.913003001
# Job requested by alex

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - master
      - main

permissions:
  id-token: write
  contents: read
  checks: read
  actions: read
  issues: read
  packages: write
  pull-requests: read
  repository-projects: read
  statuses: read

env:
  HAS_UPLOADS: 0

jobs:
  workflow:
    name: vigorous_wilson9
    runs-on: actuated-32cpu-32gb
    steps:
      - uses: actions/checkout@v1
      - name: Run the job
        run: |
          chmod +x ./job.sh
          ./job.sh



      - name: "Check for uploads"
        run: if [ -d "./uploads" ]; then echo "HAS_UPLOADS=1" >> $GITHUB_ENV ; fi

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v4
        if: ${{ env.HAS_UPLOADS == '1'}}
        with:
          if-no-files-found: ignore
          name: uploads
          path: ./uploads/
          retention-days: 1
          compression-level: 1
